package view.scenes;


/**
 * An enum, that contains every scene in the project.
 */
public enum Scenes {

    TITLE_SCENE,
    OPTIONS_SCENE,
    DUNGEON_SCENE,
    OVERWORLD_SCENE,
    SAVE_STATE_SELECTION_SCENE;
}
